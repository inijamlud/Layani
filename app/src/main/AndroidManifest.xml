<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.pei.rpl.layani"
    android:installLocation="internalOnly"
    android:sharedUserLabel="@string/app_name"
    android:versionCode="2"
    android:versionName="Beta">


    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/img_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/img_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Layani">
        <activity android:name="AC_Add" />
        <activity android:name="Pipa_Add" />
        <activity android:name="Disinfektan_Add" />
        <activity android:name=".Cleaning_Add" />
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>